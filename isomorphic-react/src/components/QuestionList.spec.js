describe(`Question List Detail Component Spec test`, () => {
    beforeEach( () =>{
        console.log("Before Each!");
    });
    beforeAll( () =>{
        console.log("Before All!");
    });
    afterEach( () =>{
        console.log("After Each!");
    });
    afterAll( () =>{
        console.log("After All!");
    });
    it(`Spec test inside`, ()=>{
        expect(2 + 2).toEqual(4);
    });
    it(`Spec test inside part two`, ()=>{
        expect(2 + 2).toEqual(4);
    });
});

